import {readFileSync,writeFileSync,mkdirSync} from 'node:fs';import{resolve}from'node:path';
const root=resolve(import.meta.dirname,'..'),items=JSON.parse(readFileSync(resolve(root,'guides/catalog.json'),'utf8'));mkdirSync(resolve(root,'guides'),{recursive:true});
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
for(const x of items){const steps=x.steps.map(s=>`<div class="step">${esc(s)}</div>`).join('');const checks=x.checks.map(s=>`<li>${esc(s)}</li>`).join('');const sources=x.sources.map(([n,u])=>`<li><a href="${u}" rel="noopener">${esc(n)}</a></li>`).join('');const html=`<!doctype html><html lang="ko"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="description" content="${esc(x.dek)}"><title>${esc(x.title)} | Atemoya 초보자 가이드</title><link rel="stylesheet" href="../assets/guide.css"><script src="../config.js"></script><script defer src="../assets/analytics.js"></script></head><body><header><div class="wrap nav"><b>ATEMOYA · 처음부터</b><a href="index.html">환경구축 시리즈</a></div></header><main><section class="hero"><div class="tag">실제로 구축하고 검증한 입문 가이드</div><h1>${esc(x.title)}</h1><p>${esc(x.dek)}</p><div class="meta"><span>${x.level}</span><span>${x.time}</span></div></section><div class="answer"><b>이 글의 목표</b><br>명령어를 외우는 것이 아니라 무엇을 왜 설치하고, 어디까지 성공하면 다음 단계로 가는지 이해하는 것입니다.</div><div class="warn"><b>보안 원칙</b><br>비밀번호·API 키·2FA 코드·SSH 개인키는 블로그, GitHub, 채팅에 입력하지 않습니다. 화면에는 가려진 예시만 사용합니다.</div><h2>따라 하는 순서</h2><section class="steps">${steps}</section><h2>완료 전에 확인하세요</h2><div class="check"><ul>${checks}</ul></div><h2>막히면 이렇게 확인하세요</h2><p>한 번에 여러 설정을 바꾸지 말고, 현재 단계의 앱이 실행 중인지와 오류 문구를 먼저 확인합니다. 오류 화면을 공유할 때는 이메일·IP·토큰·키를 가립니다.</p><h2>공식 문서</h2><ul class="sources">${sources}</ul><footer>작성 기준: 2026-08-13 · 공식 문서와 Atemoya 실제 구축 과정을 바탕으로 작성했습니다. 설치 화면과 요구사항은 업데이트될 수 있으므로 공식 문서를 최종 기준으로 확인하세요.</footer></main></body></html>`;writeFileSync(resolve(root,'guides',`${x.slug}.html`),html+'\n')}
const cards=items.map(x=>`<article><span>${x.level} · ${x.time}</span><h2><a href="${x.slug}.html">${esc(x.title)}</a></h2><p>${esc(x.dek)}</p></article>`).join('');writeFileSync(resolve(root,'guides/index.html'),`<!doctype html><html lang="ko"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Atemoya 환경구축 초보자 가이드</title><link rel="stylesheet" href="../assets/guide.css"><style>article{background:#fffaf0;border:1px solid #cfc5b2;border-radius:16px;padding:22px;margin:16px 0}article h2{margin:8px 0;font-size:26px}</style></head><body><header><div class="wrap nav"><b>ATEMOYA · 처음부터</b><a href="../index.html">홈</a></div></header><main><section class="hero"><div class="tag">BEGINNER SETUP SERIES</div><h1>AI 자동화 환경을 처음부터 만듭니다</h1><p>남는 Mac 한 대를 안전한 자동화 서버로 만드는 실제 순서입니다.</p></section>${cards}<footer>비밀번호와 인증 코드는 어떤 글에도 입력하지 마세요.</footer></main></body></html>\n`);console.log(`Generated ${items.length} beginner guides.`);
